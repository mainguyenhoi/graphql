import { makeStyles } from '@material-ui/styles';

const useStyle = makeStyles(() => ({
    root: {
        '& .MuiInputBase-root': {
            height: '44px'
        }
    },
   
}));
export default useStyle;